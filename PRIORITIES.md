# Flapjack — Priorities

**Last updated:** 2026-05-23

Canonical priority details are maintained in [`engine/docs2/1_STRATEGY/HIGHEST_PRIORITY.md`](engine/docs2/1_STRATEGY/HIGHEST_PRIORITY.md).

## Summary

All launch blockers closed. v1.0.0 released. The May 22 OSS polish wave (lanes A–F) landed: prod repo cutover to `griddlehq/flapjack` is live, nightly CI workflow parity is restored, README/CONTRIBUTING/SECURITY/CHANGELOG audits are merged, docs.flapjack.foo + flapjack-demo + apex are redeployed, and a cross-platform e2e validation pack (Linux+macOS install, Docker runtime, dashboard Playwright) is captured under `docs/research/`. Lane F's Stage 8 synthesis is `FAIL` with three named gaps (see Active), not because the engine surface is broken but because the launch-credible contracts on GHCR arm64 pull, official `algoliasearch` faceting parity, and v1.0.0 SDK registry state were not met at verdict time. Keep exact SHAs, CI runs, and sync audit details in the canonical ledger at [`engine/docs2/FEATURES.md#public-sync-lineage-ledger-canonical`](engine/docs2/FEATURES.md#public-sync-lineage-ledger-canonical).

### Completed (most recent first)

1. ~~**OpenAPI snapshot follow-through**~~ — ✅ Resolved (2026-05-23). Stage 1 confirmed the focused OpenAPI drift assertion is green at current `main`, and Stage 2 confirmed regeneration was a no-op; canonical lineage stays in [`engine/docs2/FEATURES.md#public-sync-lineage-ledger-canonical`](engine/docs2/FEATURES.md#public-sync-lineage-ledger-canonical).

2. ~~**OSS polish wave: lanes A–F**~~ — ✅ All 6 branches merged (2026-05-22 → 2026-05-23). Lane A: `griddlehq/flapjack` cutover + Cloudflare redirect + legacy archive done. Lane B: `engine/rust-toolchain.toml` pinned to 1.95.0, nightly workflow JS/Go/server-backed jobs aligned with CI parity. Lane C: root + engine READMEs polished, CONTRIBUTING/SECURITY/CHANGELOG audited, SDK READMEs cleaned, root clutter sweep + consolidated outbound-link audit done. Lane D: docs site + flapjack-demo + apex `flapjack-website` redeployed via Cloudflare Pages, ≥10-URL sitemap-first audit clean. Lane E: SDK release lane finished without registry publish; Stage 1 audit explicitly mapped JS/Python/Go canonical owners to `flapjackhq/*` and recorded `proceed-with-doc-only` / `stop` / `stop` rather than publish from this mirror. Lane F: 8 stages of e2e evidence captured; final verdict `FAIL` with three named gaps still open. Followup facet-rebuild fixes landed on `main` ([`ed6cc162`](engine/flapjack-http/src/handlers/settings.rs), [`5abbba8a`](engine/flapjack-http/src/handlers/settings.rs)) after Stage 5 evidence was written; Lane F has not been re-scored against these fixes.
3. ~~**Test hygiene, SDK contract CI, and query safety audit**~~ — ✅ Resolved with deferrals (2026-04-15). Public CI now runs the engine SDK contract tests, dashboard/browser false-positive patterns were tightened, stale `_dev/s` test shims were removed in favor of `engine/s/test`, targeted test coverage was added, and confirmed filter/experiment error paths now return typed/sanitized errors. OpenAPI snapshot follow-up remained deferred in the session handoff.
4. ~~**Analytics retention hardening + rollup foundation**~~ — ✅ Retention resolved; rollup foundation merged (2026-04-15). Partition retention is deterministic and still defaults to 90 days. Durable rollup design, known-answer query contracts, schema/config helpers, and `RollupManifest` foundation are merged. Rollup writer/query-planner/gated deletion remain open.
5. ~~**Targeted cleanup follow-through**~~ — ✅ Resolved (2026-04-08). Dashboard experiment normalization/results typing moved into `engine/dashboard/src/lib/experiment-normalization.ts`, and a stale server cognitive-complexity suppression was removed.
6. ~~**Experiment handler merge guardrails**~~ — ✅ Resolved (2026-03-31). `/2/abtests/{id}/results` now routes through the shared store-and-ID resolver seam, direct results-endpoint proofs cover store-unavailable plus numeric/UUID resolution, and experiment OpenAPI docs now declare resolver-driven `500` paths consistently. Published in the completed public lineage (see the canonical ledger in `engine/docs2/FEATURES.md`).
7. ~~**Runbook parity + admin-key truth sync**~~ — ✅ Resolved (2026-03-31). Standardized `flapjack --data-dir <path> reset-admin-key` across startup output, dashboard auth help, internal auth docs, and operator runbooks, including shell-safe quoting for spaced data-dir paths. Published in the completed public lineage (see the canonical ledger in `engine/docs2/FEATURES.md`).
8. ~~**Security baseline follow-through**~~ — ✅ Resolved (2026-03-31). Closed the scoped HTTP-hardening proof gaps by adding invalid-key non-consumption and `FLAPJACK_MAX_BODY_MB` body-limit `413` tests, aligning the security docs/audit, refreshing the committed OpenAPI export, and tightening helper-script safety around sync destinations. Published in the completed public lineage (see the canonical ledger in `engine/docs2/FEATURES.md`).
9. ~~**Security baseline docs + test coverage**~~ — ✅ Resolved (2026-03-31). `SECURITY_BASELINE.md` and the audit matrix now capture the shipped CORS/body-limit/trusted-proxy/rate-limit surface with focused proof references, and `startup_catchup.rs` now treats write-queue timeout as warn-and-continue outside strict bootstrap. Published in the completed public lineage (see the canonical ledger in `engine/docs2/FEATURES.md`).
10. ~~**Nightly CI + sync hygiene**~~ — ✅ Resolved (2026-03-31). Restored nightly Rust CI parity by creating the dashboard dist stub, added `CHANGELOG.md` / `CONTRIBUTING.md` / `SECURITY.md` to the public sync whitelist, and clarified README vector/hybrid support caveats by binary target. Published in the completed public lineage (see the canonical ledger in `engine/docs2/FEATURES.md`).
11. ~~**Operations runbook hardening**~~ — ✅ Resolved (2026-03-31). `OPERATIONS.md` now carries proof-backed startup/readiness/replication/admin-key/snapshot failure runbooks, stronger ownership links to deployment/security/config docs, verified proof citations, and the corrected `flapjack --data-dir <path> reset-admin-key` syntax. Published in the completed public lineage (see the canonical ledger in `engine/docs2/FEATURES.md`).
12. ~~**Debbie sync wave 3**~~ — ✅ Resolved (2026-03-31). Published the last wave of post-launch hardening to staging (`6166055`, CI run `23818440499`) and prod (`b7841a0`, CI run `23819698304`): public-doc sync contract, HA boundary truth surfaces, regression-gate follow-through, and the refreshed committed OpenAPI export.
13. ~~**HA convergence contract + runbook truth sync**~~ — ✅ Resolved (2026-03-31). Boundary path selected. Added `engine/docs2/4_EVIDENCE/HA_CONVERGENCE_ANALYSIS.md`, aligned `OPERATIONS.md` and the HA example README to the proven async-replication boundary, and tightened `engine/loadtest/tests/ha_soak_acceptance.sh`.
14. ~~**Tour walkthrough closure + tour typecheck unblock**~~ — ✅ Resolved (2026-03-30). Closed the former vector/chat blockers (specs 05/06), archived MP4s for all 24/24 per-feature tours, default-enabled vector/local embedding support in the build, wired local AI-provider env bridging, and fixed `npx tsc --noEmit -p tsconfig.tour.json` by adding local Node types plus aligning tour typings with the real `VideoTour` API.
15. ~~**HA multi-node soak execution + truth sync**~~ — ✅ Executed (2026-03-30). The 2h 3-node soak proof pack is now retained at `engine/loadtest/results/20260330T211227Z-ha-soak/`. Final classification was `warning-findings` with `diverged` convergence after 39 restart rotations in the nginx-routed example topology. See [`engine/loadtest/BENCHMARKS.md`](engine/loadtest/BENCHMARKS.md).
16. ~~**Cognitive complexity reduction**~~ — ✅ Resolved (2026-03-30). Decomposed 5 high-CC hotspots: `merge_settings_payload` (CC=35), `validate` (CC=29), `compute_exact_vs_prefix_bucket` (CC=26), `build_results_response` (CC=22), `browse_index` (CC=21). Added settings characterization tests.
17. ~~**Debbie sync pipeline wave 2**~~ — ✅ Resolved (2026-03-30). Full sync to staging+prod. OpenAPI test dedup, experiment handler extraction, soak proof improvements.
18. ~~**Full regression gate + targeted fixes**~~ — ✅ Resolved (2026-03-31). Ran the full post-merge regression gate, fixed FastEmbed local-embedder test nondeterminism by serializing the affected ONNX/model-cache tests, captured proof artifacts in `engine/state/`, and re-synced the committed OpenAPI export after restoring real browse/experiment endpoint summaries.
19. ~~**Public doc sync surface hardening**~~ — ✅ Resolved (2026-03-31). `.debbie.toml` now explicitly syncs the canonical public doc graph, `engine/tests/validate_sync_surface.sh` and shared doc-sync helpers are in place, `validate_doc_links.sh` covers the widened public graph, and public docs no longer refer to dev-only `_dev/s/` multi-instance helper scripts.
20. ~~**File size violations**~~ — ✅ Resolved (2026-03-29). All 20 files brought under 800-line guardrail via 13 test module extractions + 2 production code splits. 0 violations remaining.
21. ~~**GitHub release v1.0.0**~~ — ✅ Done (2026-03-29). 5 binary targets + Docker image published. Release run `23721789375`, CI run `23721442173`.
22. ~~**Debbie sync to staging**~~ — ✅ Resolved (2026-03-29). Pushed v1.0.0 + OTEL + TODO cleanup + codebase quality fixes + OpenAPI regen. CI green.
23. ~~**Post-merge regression validation**~~ — ✅ Resolved (2026-03-29). Full test suite green: 1546 (flapjack) + 1429 (flapjack-http) + server/ssl/replication crates.
24. ~~**OpenAPI spec CI fix**~~ — ✅ Resolved (2026-03-29). Root cause: 18 TODO stubs on utoipa-annotated handlers caused spec mismatch after scrai strip. Replaced with real doc comments.
25. ~~**Codebase quality cleanup**~~ — ✅ Resolved (2026-03-29). Fixed 15 error-leaking 500 sites, migrated 5 handler files to `HandlerError`, decomposed `execute_search_query`, removed cognitive_complexity suppressions.
26. ~~**TODO stub cleanup**~~ — ✅ Resolved (2026-03-29). Replaced ~601 auto-generated `TODO: Document` stubs with real doc comments. HA soak test script added.
27. ~~**Distributed tracing**~~ — ✅ Resolved (2026-03-28). OTLP gRPC export shipped behind `otel` feature flag.
28. ~~**OSS packaging**~~ — ✅ Resolved (2026-03-28). SECURITY.md, CHANGELOG.md, CONTRIBUTING.md shipped. All crates at 1.0.0.
29. ~~**Overload interpretation / tuning**~~ — ✅ Resolved (2026-03-28). Soak scenarios use `SOAK_WRITE_THRESHOLDS`.

### Active

1. **Lane F Stage 5 — Algolia faceting parity re-score** — Re-run the parity proof at current `main`; canonical status stays in [`engine/docs2/FEATURES.md#public-sync-lineage-ledger-canonical`](engine/docs2/FEATURES.md#public-sync-lineage-ledger-canonical).
2. **Lane F Stage 6 — v1.0.0 SDK publish from canonical owners** — Route publish status through the `flapjackhq/*` owners and record the result in [`engine/docs2/FEATURES.md#public-sync-lineage-ledger-canonical`](engine/docs2/FEATURES.md#public-sync-lineage-ledger-canonical).
3. **Lane F Stage 1 — GHCR arm64 pull contract** — Resolve the arm64 manifest decision and record the outcome in [`engine/docs2/FEATURES.md#public-sync-lineage-ledger-canonical`](engine/docs2/FEATURES.md#public-sync-lineage-ledger-canonical).
4. **Lane B Stage 4–5 — Post-cutover nightly verification** — Close the nightly follow-through on `griddlehq/flapjack` and record it in [`engine/docs2/FEATURES.md#public-sync-lineage-ledger-canonical`](engine/docs2/FEATURES.md#public-sync-lineage-ledger-canonical).
5. **Search HA ownership/freshness design gate** — Routing stays in [`ROADMAP.md#open--not-yet-implemented`](ROADMAP.md#open--not-yet-implemented).
6. **Durable analytics rollup writer/query planner** — Routing stays in [`ROADMAP.md#open--not-yet-implemented`](ROADMAP.md#open--not-yet-implemented).
7. **Runbooks iteration** — Operational routing stays in [`engine/docs2/3_IMPLEMENTATION/OPERATIONS.md`](engine/docs2/3_IMPLEMENTATION/OPERATIONS.md).
## Post-Launch

See [`ROADMAP.md#open--not-yet-implemented`](ROADMAP.md#open--not-yet-implemented) for post-launch work (search HA design, durable analytics rollups, runbooks, mobile dashboard, OWASP deep pass).
